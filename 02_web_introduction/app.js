// Web Introduction & TCP/IP, HTTP, DNS, Browsers - app.js (gol, folosește pentru exerciții)
console.log("Chapter: Web Introduction & TCP/IP, HTTP, DNS, Browsers");

function toggleAnswer(id) {
  const el = document.getElementById(id);
  el.classList.toggle('hidden');
}
